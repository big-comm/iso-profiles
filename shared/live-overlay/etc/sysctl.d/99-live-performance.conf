# Sysctl tuning for live mode performance
# Live runs from squashfs overlay in RAM — optimize for that

# Reduce swap tendency (RAM is primary storage in live)
vm.swappiness=10

# Reduce dirty page writeback pressure (writes go to tmpfs overlay)
vm.dirty_ratio=40
vm.dirty_background_ratio=10
